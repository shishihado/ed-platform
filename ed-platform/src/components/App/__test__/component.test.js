import assert from 'assert';
import { shallow } from 'enzyme';

import App from '..';

describe('<App /> component', () => {
  it('should has header', () => {
    // let wrapper = shallow( <App /> );
    // assert.equal(wrapper.contains(<Header />), true);
  });
});
